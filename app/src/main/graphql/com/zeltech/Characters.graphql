query Characters($name: String, $status: String, $gender: String) {
    characters(filter: {name: $name, status: $status gender: $gender}){
        info {
            count
            pages
            next
            prev
        }
        results {
            id
            name
            image
        }
    }
}
