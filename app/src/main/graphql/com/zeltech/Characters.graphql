query Characters($name: String, $gender: String) {
    characters(filter: {name: $name, gender: $gender}){
        info {
            count
            pages
            next
            prev
        }
        results {
            id
            name
            image
        }
    }
}
