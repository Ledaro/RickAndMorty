query Characters($name: String) {
    characters(filter: {name: $name}){
        info {
            count
            pages
            next
            prev
        }
        results {
            id
            name
            image
        }
    }
}
